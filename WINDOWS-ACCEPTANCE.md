# Windows 人工验收清单

以下检查需要 Windows 10/11 x64 及真实键鼠／XInput 手柄。未在 macOS 开发环境中代替执行。请记录系统版本、应用版本、实际结果和日志。建议用真实游戏，也可以添加 Notepad 等程序的独立可执行文件作为可控的测试对象；Windows 11 的应用包装进程需要规则指向实际窗口所属 exe。

| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 解压整个发布目录，运行 `GameActivityTracker.exe` | 出现空 Dashboard，`%LOCALAPPDATA%\GameActivityTracker\activity.db` 自动创建 |
| 2 | 执行 `scripts/smoke-test.ps1` | 独立测试数据目录启动，4 秒后正常退出，脚本显示 PASS |
| 3 | 通过 exe 选择器添加游戏，关闭再重新启动记录器 | 游戏与匹配规则仍存在 |
| 4 | 启动所添加游戏（或先启动游戏再启动记录器） | 默认约 2 秒内建立一个会话，从观察时刻开始计时 |
| 5 | 保持游戏前台且使用键鼠 | 状态 ACTIVE，Active 与 Running 增长 |
| 6 | 停止输入满 60 秒 | 最后输入后的前 60 秒继续 Active；随后变为 IDLE，仅 Running 增长 |
| 7 | 重新输入 | 下一次约 250ms 采样后恢复 ACTIVE |
| 8 | 切到浏览器打字 | 游戏 BACKGROUND，Active 不增加 |
| 9 | 切回游戏且有近期输入 | 游戏重新 ACTIVE |
| 10 | 用 XInput 手柄改变按钮／摇杆／扳机，不触碰键鼠 | 游戏保持 ACTIVE；放下手柄且仅死区内漂移，满阈值后 IDLE |
| 11 | 游戏运行时锁屏；锁屏期间结束游戏（如游戏可自动退出） | 不计 Active；继续扫描并识别进程退出 |
| 12 | 游戏运行时睡眠一段时间，再恢复 | 间隔为 UNKNOWN，不增加 Active；Running 包含观察间隔 |
| 13 | 同一游戏配置两个进程 | 一个退出而另一个仍在时会话保持；全部退出才结束 |
| 14 | 关闭主窗口 | 默认隐藏到托盘，记录仍持续；双击托盘重新打开 |
| 15 | 托盘右键 Exit，重新打开 | 退出前的会话与状态区间保留 |
| 16 | 游戏运行时用任务管理器结束记录器进程，再重开 | 旧会话截止最后 checkpoint，恢复标记正确，离线时间不记 Active，新观察建立新会话 |
| 17 | 点击热力图日期 | 当地自然日 Active / Running 与分游戏记录一致 |
| 18 | Games 中双击游戏 | 游戏统计、年度热力图和最近会话显示 |
| 19 | 修改阈值／扫描间隔／死区并保存 | 新设置立即应用并在重启后保留，原始历史区间不被修改 |
| 20 | 输入无效设置，例如 `abc`、0 秒、死区 2 | 提示错误，不静默按旧值保存 |
| 21 | 开启登录启动，确保 exe 已在固定目录，再登出登录 | 记录器后台启动且托盘出现；关闭选项后注册项移除 |
| 22 | 保持记录器运行超过 24 小时并跨本地午夜 | 连续会话分摊到两天，不重复累计；日志／数据库可继续读写 |

通过托盘 Exit 关闭应用后可备份整个数据目录。查询数据库时注意：时长是 Segments 的 UTC 时间差聚合，不是单独维护的累计字段；不存在 `RunningDuration` 数值列是设计选择。
